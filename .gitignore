renv/library/
renv/staging/
rsconnect/
.Rhistory
.RData
.Ruserdata

# Large upstream boundary files (NCES SABS) are manually downloaded and should
# not be committed. The app uses the derived lightweight JSON under app/data/.
data-raw/input/sabs/
